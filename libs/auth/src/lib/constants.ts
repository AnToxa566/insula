export const IS_PUBLIC_KEY = 'isPublic';

// Opt-in metadata key for @AllowAgent(). Absence means "reject agent
// tokens" — see the decorator for why opt-in is the safe direction here.
export const ALLOW_AGENT_KEY = 'allowAgent';
